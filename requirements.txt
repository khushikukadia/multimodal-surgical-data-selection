# Core scientific stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=10.0
tqdm>=4.65

# PyTorch (CPU is fine for frozen feature extraction).
# If you need CUDA, install per the instructions at https://pytorch.org/get-started/locally/
torch>=2.0
torchvision>=0.15

# CLIP encoder (preferred). If install is difficult, fall back to ResNet:
#   python scripts/run_experiment.py --embedding_backend resnet
open-clip-torch>=2.20
